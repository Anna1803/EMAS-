Drop the current `EMAS_data_exported.csv` export from Cority in this folder.
This folder's CSV contents are intentionally excluded from version control
(see .gitignore) since the data contains real staff names.
